{
  "name": "cpwire",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "cp|WIRE — cockpit PMO/MOE. Monorepo : web/ (front React+Vite), server/ (API Express), shared/ (charte). Chaque sous-dossier a son propre package.json ; ce manifeste racine n'est pas publié et sert uniquement à identifier la racine du dépôt."
}
